doctype 5
html
	head
		title= title
		meta(charset='utf-8')
		meta(name='viewport', content='initial-scale=1.0, user-scalable=no')
		script(src='//code.jquery.com/jquery.min.js')
		script(src='//ajax.microsoft.com/ajax/jquery.templates/beta1/jquery.tmpl.min.js')
		script(src='//cdnjs.cloudflare.com/ajax/libs/underscore.js/1.4.2/underscore-min.js')
		script(src='//cdnjs.cloudflare.com/ajax/libs/backbone.js/0.9.2/backbone-min.js')
		script(src='//netdna.bootstrapcdn.com/twitter-bootstrap/2.3.1/js/bootstrap.min.js')
		script(src='/javascripts/manga.js')
		script(src='/javascripts/bootbox.min.js')


		link(rel='stylesheet', href="//netdna.bootstrapcdn.com/twitter-bootstrap/2.3.1/css/bootstrap.min.css")
		style
			body {
				padding-top: 60px; /* 60px to make the container go all the way to the bottom of the topbar */
				}
		link(href="//netdna.bootstrapcdn.com/twitter-bootstrap/2.3.1/css/bootstrap-responsive.min.css", rel="stylesheet")
		link(rel='stylesheet', href='/stylesheets/manga.css')

		body
			.navbar.navbar-inverse.navbar-fixed-top
				.navbar-inner
					.container
						ul.nav
							li
								a(href='/local') Local
							li
								a(href='/mangafox') Mangafox


			.container
				h2 #{manga.title}
				h5 #{manga.author}
				img.image(src="#{manga.image}").pull-left
				#description !{manga.description}
				br(style="clear:both")
				br
				br

				ul#volumelist
					for volume in manga.volumes
						li.volume(id='#{volume.id}')
								div.volumetop
									a.volumelink(href='#') #{volume.title}
									i.icon-refresh.icon-white
								ul.chapterlist
									for chapter in volume.chapterlist
										li.chapter(id='#{chapter.id}')
											a.chapterlink(href='#', onclick="bootbox.confirm('test', function(result){ if(result) window.open('/data/testjentencrazy.mobi');});return false;", data-url='#{chapter.url}') #{chapter.title}
											i.icon-refresh
//- Manga.downloadChaptersFromList(['#{chapter.url}'])




